type Query {
    users: [User]
    userById(id: ID): User
    userByEmail(email: String): User
}

type User {
    id: ID!
    email: String!
    groups: [Group]
}

type Group {
    id: ID!
    owner: User
    title: String!
    description: String
    #members: [User]
}
