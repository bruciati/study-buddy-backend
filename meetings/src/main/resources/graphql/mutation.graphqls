type Mutation {

    # Meeting
    addMeeting(input: MeetingInput): Meeting
    updateMeeting(id: ID!, input: MeetingInput): Meeting
    removeMeeting(id: ID!): Meeting

    #User
    addUserToMeeting(user: ID!, meeting_id: ID!): Attendees

}

